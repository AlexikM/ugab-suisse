{
  "name": "ugab-suisse",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "typecheck": "astro check",
    "lint": "biome check .",
    "format": "biome check --write .",
    "test": "npm run test:build && npm run test:site",
    "test:e2e": "playwright test",
    "check": "npm run typecheck && npm run lint && npm run build && npm run test && npm run test:e2e",
    "test:build": "vitest run",
    "test:site": "node --test tests/content/*.test.mjs tests/compliance/*.test.mjs"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.0",
    "astro": "^6.3.8",
    "tailwindcss": "^4.3.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@biomejs/biome": "2.5.9",
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.2.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.11"
  }
}
